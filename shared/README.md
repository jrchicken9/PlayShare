# Shared config

- **`streaming-hosts.json`** — canonical list of streaming host substrings and Chrome `matches` patterns.
- After editing it, run **`npm run sync-streaming`** to regenerate `streaming-hosts.generated.js` and update `manifest.json`.
