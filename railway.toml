# https://docs.railway.com/deploy/config-as-code
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 300
# Avoid cold-start / sleep 502s on hobby tiers when the platform supports it.
sleepApplication = false
